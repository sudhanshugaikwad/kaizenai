
'use client';

import * as React from 'react';

// This component is no longer used as the theme is forced to dark mode.
// Returning null to avoid breaking imports.
export function ThemeToggle() {
  return null;
}
